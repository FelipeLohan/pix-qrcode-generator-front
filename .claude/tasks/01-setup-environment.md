# Task 01: Setup do Ambiente e Estrutura Inicial
- [x] Criar o projeto Angular 21 (se não existir) com `--standalone`.
- [x] Configurar o `proxy.conf.json` para redirecionar `/api` para `http://localhost:8080`.
- [x] Configurar o `provideHttpClient()` no `app.config.ts`.
- [x] Criar as pastas de estrutura: `models/`, `services/`, `components/`.
